# Relevance AI API Key - Required for authentication
# Get this from your Relevance AI dashboard
RELEVANCE_API_KEY=your_relevance_api_key_here

# Relevance AI API Base URL - Optional, defaults to https://api.relevanceai.com/v1
# Only change this if you're using a custom deployment or staging environment
RELEVANCE_API_BASE_URL=https://api.relevanceai.com/v1

# Server port - Optional, defaults to 8000
# Most cloud platforms (Railway, Render, etc.) will override this automatically
PORT=8000

# CORS Origins - Optional, comma-separated list of allowed origins
# Leave empty or set to * to allow all origins
# For production, set to specific domains like: https://studio.relevance.ai,https://yourdomain.com
CORS_ORIGINS=*

# Log level - Optional, defaults to INFO
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO
